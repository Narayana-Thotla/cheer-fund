/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack: (config, { dev, isServer }) => {
    //     if (!dev) {
    //       config.devtool = isServer ? false : 'source-map'; // Replace 'source-map' with your custom devtool if needed
    //     }
    //     return config;
    //   },
    
};

export default nextConfig;
